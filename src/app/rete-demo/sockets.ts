import { Socket } from 'rete';

export const numSocket = new Socket('Number value');
export const fileSocket = new Socket('File value');
export const fileArraySocket = new Socket('File array value');
